## 斜杠指令

斜杆指令，就是您在编辑区中输入斜杆`/`符号之后，在该行位置会出现一个指令面板。

![image-20251023154339570](https://images.waer.ltd/notes/202510231546262.png)

当前最新版中，斜杠指令中集成了几乎绝大部分常用的 `markdown` 语法指令，即使您对 `markdown` 语法一无所知，也可以在创建文章的实际通过斜杠指令快速插入各类语法格式。

> [!NOTE]
>
> 随着后续版本的更新迭代，斜杠菜单中将会集成更多的功能，敬请期待。
